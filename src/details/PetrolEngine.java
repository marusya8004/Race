package details;

public class PetrolEngine extends Engine {
        private double gasMileage;

    public PetrolEngine(String name, double maxSpeed) {
        super(name, maxSpeed);
    }
}
